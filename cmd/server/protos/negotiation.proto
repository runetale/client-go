syntax = "proto3";

package protos;

option go_package = "./negotiation";

service Negotiation {
	rpc ConnectStream(stream StreamMessage) returns (stream StreamMessage) {}
}

message StreamMessage {
	string privateKey = 1;
	string clientMachineKey = 2;
}
